{
  "name": "root",
  "private": true,
  "workspaces": [
    "core",
    "examples"
  ],
  "scripts": {
    "build": "npm run-script build --workspace @carefrees/simple-menu",
    "version": "lerna version --exact --force-publish --no-push --no-git-tag-version"
  },
  "devDependencies": {
    "lerna": "^7.0.1",
    "tsbb": "~4.1.14"
  }
}